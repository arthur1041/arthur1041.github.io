<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Distributed Systems on Arthur Cruz</title><link>https://arthcruz.dev/pt-br/categories/distributed-systems/</link><description>Recent content in Distributed Systems on Arthur Cruz</description><generator>Hugo -- gohugo.io</generator><language>pt-br</language><lastBuildDate>Sat, 28 Mar 2026 00:00:00 +0000</lastBuildDate><atom:link href="https://arthcruz.dev/pt-br/categories/distributed-systems/index.xml" rel="self" type="application/rss+xml"/><item><title>O que é uma Race Condition e Como Lidar com Ela</title><link>https://arthcruz.dev/pt-br/posts/what_is_a_race_condition_and_how_to_deal_with_it/</link><pubDate>Sat, 28 Mar 2026 00:00:00 +0000</pubDate><guid>https://arthcruz.dev/pt-br/posts/what_is_a_race_condition_and_how_to_deal_with_it/</guid><description>&lt;img src="https://arthcruz.dev/en/posts/what_is_a_race_condition_and_how_to_deal_with_it/images/rc_poster.png" alt="Featured image of post O que é uma Race Condition e Como Lidar com Ela" />&lt;h2 id="1-introdução-hook">1. Introdução (Hook)
&lt;/h2>&lt;p>Recentemente, enquanto trabalhava em um projeto, encontrei um problema de &lt;strong>race condition&lt;/strong>. Esse problema específico, frequentemente visto em cenários de alta concorrência como e-commerce (ex: dois usuários tentando comprar o último item simultaneamente) ou sistemas de reserva (ex: reservas de propriedades sobrepostas), manifestou-se quando usuários tentaram adicionar dois registros simultaneamente, ignorando uma validação de &lt;em>check-before-insert&lt;/em>.&lt;/p>
&lt;p>Tais validações, projetadas para evitar duplicatas, podem falhar inesperadamente, especialmente quando uma aplicação opera em múltiplos &lt;em>pods&lt;/em> ou &lt;em>containers&lt;/em> paralelos. Quando encontrados pela primeira vez, a natureza inerente desses &lt;em>bugs&lt;/em> pode ser sutil e confusa. Eles geralmente levam a sintomas como registros duplicados ou dados inconsistentes, e seu comportamento não determinístico sob carga pesada e concorrência os torna notoriamente difíceis de reproduzir e depurar.&lt;/p>
&lt;p>Isso destaca por que entender &lt;strong>race conditions&lt;/strong> é crítico em sistemas reais e por que implementar estratégias robustas para gerenciá-las efetivamente em ambientes de produção é primordial.&lt;/p>
&lt;h2 id="2-o-que-é-uma-race-condition">2. O que é uma Race Condition?
&lt;/h2>&lt;p>Uma &lt;strong>race condition&lt;/strong> ocorre quando a correção de um programa depende do tempo ou do entrelaçamento de múltiplas operações concorrentes. Essencialmente, é uma falha onde a saída de um sistema é inesperadamente afetada pela sequência ou tempo de outros eventos incontroláveis. Isso geralmente acontece quando múltiplos processos ou &lt;em>threads&lt;/em> acessam e modificam recursos compartilhados sem a sincronização adequada, levando a resultados imprevisíveis e, muitas vezes, incorretos.&lt;/p>
&lt;h2 id="3-exemplo-simples">3. Exemplo Simples
&lt;/h2>&lt;p>Para ilustrar o conceito central de uma &lt;strong>race condition&lt;/strong>, particularmente o padrão &lt;em>&amp;lsquo;check-then-act&amp;rsquo;&lt;/em> prevalente em cenários como gerenciamento de inventário de e-commerce ou sistemas de reserva, considere um exemplo simplificado. Imagine um serviço projetado para garantir que um usuário tenha apenas uma entrada única em um banco de dados. Uma abordagem aparentemente lógica, porém falha, envolveria verificar a existência da entrada e, se ausente, prosseguir com a inserção:&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-java" data-lang="java">&lt;span style="display:flex;">&lt;span>&lt;span style="color:#66d9ef">public&lt;/span> &lt;span style="color:#66d9ef">class&lt;/span> &lt;span style="color:#a6e22e">UserService&lt;/span> {
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#66d9ef">private&lt;/span> Database database; &lt;span style="color:#75715e">// Suponha que esta seja uma interface de banco de dados simplificada&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#66d9ef">public&lt;/span> &lt;span style="color:#66d9ef">void&lt;/span> &lt;span style="color:#a6e22e">createUserEntry&lt;/span>(String userId) {
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#66d9ef">if&lt;/span> (&lt;span style="color:#f92672">!&lt;/span>database.&lt;span style="color:#a6e22e">userEntryExists&lt;/span>(userId)) {
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#75715e">// Simula algum tempo de processamento&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> &lt;span style="color:#66d9ef">try&lt;/span> { Thread.&lt;span style="color:#a6e22e">sleep&lt;/span>(100); } &lt;span style="color:#66d9ef">catch&lt;/span> (InterruptedException e) { Thread.&lt;span style="color:#a6e22e">currentThread&lt;/span>().&lt;span style="color:#a6e22e">interrupt&lt;/span>(); }
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> database.&lt;span style="color:#a6e22e">insertUserEntry&lt;/span>(userId);
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> System.&lt;span style="color:#a6e22e">out&lt;/span>.&lt;span style="color:#a6e22e">println&lt;/span>(&lt;span style="color:#e6db74">&amp;#34;Entrada de usuário para &amp;#34;&lt;/span> &lt;span style="color:#f92672">+&lt;/span> userId &lt;span style="color:#f92672">+&lt;/span> &lt;span style="color:#e6db74">&amp;#34; criada.&amp;#34;&lt;/span>);
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> } &lt;span style="color:#66d9ef">else&lt;/span> {
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> System.&lt;span style="color:#a6e22e">out&lt;/span>.&lt;span style="color:#a6e22e">println&lt;/span>(&lt;span style="color:#e6db74">&amp;#34;Entrada de usuário para &amp;#34;&lt;/span> &lt;span style="color:#f92672">+&lt;/span> userId &lt;span style="color:#f92672">+&lt;/span> &lt;span style="color:#e6db74">&amp;#34; já existe.&amp;#34;&lt;/span>);
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> }
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span> }
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>}
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#75715e">// Em um ambiente concorrente:&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#75715e">// Thread A chama createUserEntry(&amp;#34;user1&amp;#34;)&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#75715e">// Thread B chama createUserEntry(&amp;#34;user1&amp;#34;)&lt;/span>
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>Em um ambiente concorrente, essa sequência &lt;em>&amp;lsquo;check-then-act&amp;rsquo;&lt;/em> torna-se problemática. Se a &lt;code>Thread A&lt;/code> verificar &lt;code>userEntryExists&lt;/code> e não encontrar nenhuma entrada, e a &lt;code>Thread B&lt;/code> realizar a mesma verificação &lt;em>antes&lt;/em> que a &lt;code>Thread A&lt;/code> tenha completado sua operação &lt;code>insertUserEntry&lt;/code>, ambas as &lt;em>threads&lt;/em> concluirão erroneamente que nenhuma entrada existe. Consequentemente, ambas prosseguirão para inserir a entrada do usuário, levando a registros duplicados e violando o estado pretendido do sistema. Isso é análogo a dois clientes verificando simultaneamente o último produto disponível e ambos sendo informados de que está em estoque, apenas para que a compra de um falhe ou ocorra uma venda excessiva (&lt;em>oversell&lt;/em>).&lt;/p>
&lt;h2 id="4-seção-de-visualização">4. Seção de Visualização
&lt;/h2>&lt;style>
pre.mermaid {
background-color: #FAFAFA;
}
&lt;/style>
&lt;pre class="mermaid">
sequenceDiagram
participant A as Thread A (Requisição 1)
participant B as Thread B (Requisição 2)
participant DB as Banco de Dados
A-&amp;gt;&amp;gt;DB: SELECT: user1 existe?
B-&amp;gt;&amp;gt;DB: SELECT: user1 existe?
DB--&amp;gt;&amp;gt;A: Nenhuma entrada encontrada
DB--&amp;gt;&amp;gt;B: Nenhuma entrada encontrada
Note over A,B: Ambas as threads acreditam que não existe entrada
A-&amp;gt;&amp;gt;DB: INSERT user1
B-&amp;gt;&amp;gt;DB: INSERT user1
DB--&amp;gt;&amp;gt;A: ✅ Sucesso
DB--&amp;gt;&amp;gt;B: ⚠️ Sucesso (duplicata criada)
Note over DB: Integridade de dados violada (sem constraint)
&lt;/pre>
&lt;p>Este diagrama ilustra duas requisições concorrentes tentando criar uma entrada de usuário. Ambas as requisições leem o estado compartilhado (verificando se o usuário existe) aproximadamente ao mesmo tempo, não encontram nenhuma entrada existente e, em seguida, ambas prosseguem para escrever (inserir a entrada do usuário), levando a uma &lt;strong>race condition&lt;/strong>.&lt;/p>
&lt;h2 id="5-por-que-race-conditions-acontecem">5. Por que Race Conditions Acontecem
&lt;/h2>&lt;p>&lt;strong>Race conditions&lt;/strong> são um subproduto comum de arquiteturas de software modernas, principalmente devido a:&lt;/p>
&lt;ul>
&lt;li>&lt;strong>Concorrência:&lt;/strong> A execução simultânea de múltiplas &lt;em>threads&lt;/em>, processos ou serviços distribuídos que compartilham recursos. Isso é inerente a aplicações &lt;em>multi-threaded&lt;/em>, servidores web lidando com múltiplas requisições e arquiteturas de microserviços.&lt;/li>
&lt;li>&lt;strong>Falta de Sincronização:&lt;/strong> Quando o acesso a recursos compartilhados (como registros de banco de dados, &lt;em>caches&lt;/em> em memória ou arquivos) não é devidamente coordenado, permitindo que múltiplas operações interfiram umas nas outras.&lt;/li>
&lt;li>&lt;strong>Suposição Incorreta de Atomicidade:&lt;/strong> Desenvolvedores às vezes assumem que uma sequência de operações (ex: &lt;em>read-modify-write&lt;/em>) será executada como uma unidade única e indivisível, quando na realidade, elas podem ser interrompidas e entrelaçadas por outras operações.&lt;/li>
&lt;/ul>
&lt;p>Sistemas modernos, especialmente aqueles construídos com APIs e microserviços, aumentam significativamente a exposição a &lt;strong>race conditions&lt;/strong> devido à sua natureza distribuída e altamente concorrente.&lt;/p>
&lt;h2 id="6-tipos-de-race-conditions">6. Tipos de Race Conditions
&lt;/h2>&lt;p>&lt;strong>Race conditions&lt;/strong> manifestam-se de várias formas, mas as mais críticas frequentemente envolvem um padrão &lt;em>&amp;lsquo;check-then-act&amp;rsquo;&lt;/em>, onde uma decisão é tomada com base em um estado percebido que pode mudar antes que a ação seja concluída. Dois tipos comuns e impactantes são:&lt;/p>
&lt;h3 id="61-check-then-act-race-condition-ex-inserções-duplicadas-venda-excessiva-de-inventário-reservas-sobrepostas">6.1 Check-Then-Act Race Condition (ex: Inserções Duplicadas, Venda Excessiva de Inventário, Reservas Sobrepostas)
&lt;/h3>&lt;p>Este tipo de &lt;strong>race condition&lt;/strong> é precisamente o que discutimos no exemplo simples. Ocorre quando múltiplas operações concorrentes realizam uma verificação (ex: &amp;ldquo;este produto está em estoque?&amp;rdquo; ou &amp;ldquo;este horário está disponível?&amp;rdquo;) e, com base nessa verificação, prosseguem para agir (ex: &amp;ldquo;decrementar estoque&amp;rdquo; ou &amp;ldquo;reservar horário&amp;rdquo;). Se o estado mudar entre a verificação e a ação devido a outra operação concorrente, o sistema pode acabar em um estado inconsistente. Isso leva a problemas como &lt;strong>registros duplicados&lt;/strong>, &lt;strong>venda excessiva de inventário&lt;/strong> ou &lt;strong>reserva dupla de recursos&lt;/strong>, que podem ter implicações comerciais significativas.&lt;/p>
&lt;h3 id="62-update-race-condition-lost-update">6.2 Update Race Condition (Lost Update)
&lt;/h3>&lt;p>Uma &lt;strong>update race condition&lt;/strong>, frequentemente referida como &lt;strong>lost update&lt;/strong>, ocorre quando duas ou mais operações concorrentes tentam modificar o mesmo dado, e uma atualização sobrescreve outra sem incorporar suas alterações. Por exemplo, se dois usuários tentarem atualizar simultaneamente a quantidade de um item em um sistema de inventário, e ambos lerem a quantidade atual, realizarem um cálculo e, em seguida, escreverem a nova quantidade, uma das atualizações será perdida. Embora ainda seja uma preocupação, estas são frequentemente tratadas de forma diferente com mecanismos de bloqueio padrão em comparação com os cenários &lt;em>&amp;lsquo;check-then-act&amp;rsquo;&lt;/em>, onde a própria verificação inicial é vulnerável.&lt;/p>
&lt;h2 id="7-estratégias-para-resolver-race-conditions">7. Estratégias para Resolver Race Conditions
&lt;/h2>&lt;p>Não existe uma solução única e universal para todas as &lt;strong>race conditions&lt;/strong>. A melhor abordagem depende do contexto específico, do tipo de recurso compartilhado e dos requisitos de desempenho. Aqui estão várias estratégias eficazes:&lt;/p>
&lt;h3 id="71-database-constraints">7.1 Database Constraints
&lt;/h3>&lt;p>Para prevenir &lt;strong>race conditions&lt;/strong> de inserção e garantir a integridade dos dados, as &lt;strong>unique constraints&lt;/strong> do banco de dados são frequentemente a &lt;em>única garantia confiável de correção&lt;/em> sob escritas concorrentes. Ao definir uma &lt;em>unique constraint&lt;/em> em uma ou mais colunas, o próprio banco de dados impõe a integridade dos dados no nível mais fundamental. Se uma inserção concorrente tentar criar uma duplicata, o banco de dados lançará um erro, que a aplicação poderá tratar graciosamente. É um princípio crítico a ser lembrado: &lt;strong>Verificações em nível de aplicação não são garantias sob concorrência.&lt;/strong>&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-sql" data-lang="sql">&lt;span style="display:flex;">&lt;span>&lt;span style="color:#66d9ef">ALTER&lt;/span> &lt;span style="color:#66d9ef">TABLE&lt;/span> user_entries &lt;span style="color:#66d9ef">ADD&lt;/span> &lt;span style="color:#66d9ef">CONSTRAINT&lt;/span> unique_user_id &lt;span style="color:#66d9ef">UNIQUE&lt;/span> (user_id);
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>Esta abordagem transfere a responsabilidade da correção para o banco de dados, que é altamente otimizado para tais tarefas. É uma verdade fundamental no design de sistemas concorrentes: &lt;strong>Na maioria dos sistemas, o banco de dados é a única camada que pode garantir de forma confiável a consistência sob escritas concorrentes — todo o resto é o melhor esforço (&lt;em>best-effort&lt;/em>).&lt;/strong>&lt;/p>
&lt;h3 id="72-transactions--locks-check-then-insert-com-locking">7.2 Transactions + Locks (Check-Then-Insert com Locking)
&lt;/h3>&lt;p>Para cenários onde as &lt;em>constraints&lt;/em> do banco de dados sozinhas são insuficientes, ou para operações de &lt;em>read-modify-write&lt;/em> mais complexas, &lt;strong>transações de banco de dados combinadas com bloqueio explícito&lt;/strong> podem ser usadas. É crucial entender que o &lt;code>SELECT FOR UPDATE&lt;/code> bloqueia apenas linhas &lt;em>existentes&lt;/em>. Esta abordagem NÃO evita &lt;strong>race conditions&lt;/strong> de inserção quando a linha ainda não existe; uma &lt;em>unique constraint&lt;/em> ainda é necessária. O &lt;code>SELECT FOR UPDATE&lt;/code> é eficaz para bloquear linhas que se &lt;em>espera&lt;/em> que existam e sejam modificadas. Por exemplo, em bancos de dados SQL, o &lt;code>SELECT FOR UPDATE&lt;/code> pode ser usado para adquirir um bloqueio exclusivo em uma linha (ou linhas) &lt;em>existente&lt;/em> antes de realizar uma atualização, garantindo que nenhuma outra transação possa modificar ou bloquear essa linha até que a transação atual seja confirmada ou revertida.&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-sql" data-lang="sql">&lt;span style="display:flex;">&lt;span>&lt;span style="color:#75715e">-- Transação 1: Bloqueando uma linha existente para atualização
&lt;/span>&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#75715e">&lt;/span>&lt;span style="color:#66d9ef">BEGIN&lt;/span>;
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#66d9ef">SELECT&lt;/span> &lt;span style="color:#f92672">*&lt;/span> &lt;span style="color:#66d9ef">FROM&lt;/span> products &lt;span style="color:#66d9ef">WHERE&lt;/span> id &lt;span style="color:#f92672">=&lt;/span> &lt;span style="color:#ae81ff">123&lt;/span> &lt;span style="color:#66d9ef">FOR&lt;/span> &lt;span style="color:#66d9ef">UPDATE&lt;/span>;
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#66d9ef">UPDATE&lt;/span> products &lt;span style="color:#66d9ef">SET&lt;/span> quantity &lt;span style="color:#f92672">=&lt;/span> quantity &lt;span style="color:#f92672">-&lt;/span> &lt;span style="color:#ae81ff">1&lt;/span> &lt;span style="color:#66d9ef">WHERE&lt;/span> id &lt;span style="color:#f92672">=&lt;/span> &lt;span style="color:#ae81ff">123&lt;/span>;
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#66d9ef">COMMIT&lt;/span>;
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#75715e">-- Transação 2 (aguardará a Transação 1 confirmar ou reverter se tentar adquirir o mesmo bloqueio no produto id 123)
&lt;/span>&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>Isso garante que apenas uma transação possa operar nos dados bloqueados por vez, fornecendo exclusão mútua e prevenindo &lt;strong>race conditions&lt;/strong> em registros &lt;em>existentes&lt;/em>. É importante notar que, embora as transações forneçam atomicidade (tudo ou nada), os bloqueios (ex: &lt;code>SELECT FOR UPDATE&lt;/code>) fornecem exclusão mútua e coordenação. Níveis de isolamento adequados e/ou mecanismos de bloqueio explícitos são necessários para alcançar o controle total de concorrência e evitar &lt;strong>race conditions&lt;/strong>.&lt;/p>
&lt;h4 id="721-níveis-de-isolamento-de-transação">7.2.1 Níveis de Isolamento de Transação
&lt;/h4>&lt;p>Os níveis de isolamento de transação do banco de dados desempenham um papel crítico em como as &lt;strong>race conditions&lt;/strong> são tratadas. O isolamento &lt;code>READ COMMITTED&lt;/code>, o padrão para muitos bancos de dados, não evita &lt;strong>race conditions&lt;/strong> como &lt;em>lost updates&lt;/em> ou leituras não repetíveis. O &lt;code>REPEATABLE READ&lt;/code> oferece garantias mais fortes, evitando leituras não repetíveis, mas ainda permitindo &lt;strong>race conditions&lt;/strong> de inserção (&lt;em>phantom reads&lt;/em>) conforme definido pelo padrão SQL. O nível de isolamento mais alto, &lt;code>SERIALIZABLE&lt;/code>, evita efetivamente todas as &lt;strong>race conditions&lt;/strong> ao garantir que as transações sejam executadas como se fossem rodadas sequencialmente, mas isso vem com um impacto significativo no desempenho devido ao aumento de bloqueios e contenção.&lt;/p>
&lt;blockquote>
&lt;p>&lt;strong>Nota:&lt;/strong> A implementação de &lt;code>REPEATABLE READ&lt;/code> do PostgreSQL é mais forte do que o padrão SQL exige. Como utiliza &lt;em>snapshot isolation&lt;/em> em vez de bloqueios de intervalo (&lt;em>range locks&lt;/em>), ele também evita &lt;em>phantom reads&lt;/em> na prática — comportamento que o padrão só garante em &lt;code>SERIALIZABLE&lt;/code>. Se você estiver trabalhando exclusivamente com PostgreSQL, o &lt;code>REPEATABLE READ&lt;/code> pode ser suficiente para cenários onde outros bancos de dados exigiriam &lt;code>SERIALIZABLE&lt;/code>.&lt;/p>
&lt;/blockquote>
&lt;h3 id="73-upsert-insert--on-conflict--on-duplicate-key">7.3 UPSERT (INSERT &amp;hellip; ON CONFLICT / ON DUPLICATE KEY)
&lt;/h3>&lt;p>&lt;strong>UPSERT&lt;/strong> (uma aglutinação de &amp;ldquo;UPDATE&amp;rdquo; e &amp;ldquo;INSERT&amp;rdquo;) é uma solução comum e altamente eficaz no mundo real para evitar inserções duplicadas e lidar com atualizações concorrentes, especialmente quando uma &lt;em>unique constraint&lt;/em> está presente. Esse padrão permite que uma aplicação tente uma inserção e, se surgir um conflito devido a uma chave única existente, ela pode não fazer nada ou atualizar a linha existente. Isso depende diretamente da capacidade do banco de dados de lidar com o conflito de forma atômica, tornando-o mais simples e seguro do que a lógica de &lt;em>check-then-act&lt;/em> em nível de aplicação.&lt;/p>
&lt;p>&lt;strong>Exemplo PostgreSQL:&lt;/strong>&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-sql" data-lang="sql">&lt;span style="display:flex;">&lt;span>&lt;span style="color:#66d9ef">INSERT&lt;/span> &lt;span style="color:#66d9ef">INTO&lt;/span> user_entries (user_id)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#66d9ef">VALUES&lt;/span> (&lt;span style="color:#e6db74">&amp;#39;user1&amp;#39;&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#66d9ef">ON&lt;/span> CONFLICT (user_id) &lt;span style="color:#66d9ef">DO&lt;/span> &lt;span style="color:#66d9ef">NOTHING&lt;/span>;
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>&lt;strong>Exemplo MySQL:&lt;/strong>&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-sql" data-lang="sql">&lt;span style="display:flex;">&lt;span>&lt;span style="color:#66d9ef">INSERT&lt;/span> &lt;span style="color:#66d9ef">INTO&lt;/span> user_entries (user_id)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#66d9ef">VALUES&lt;/span> (&lt;span style="color:#e6db74">&amp;#39;user1&amp;#39;&lt;/span>)
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#66d9ef">ON&lt;/span> DUPLICATE &lt;span style="color:#66d9ef">KEY&lt;/span> &lt;span style="color:#66d9ef">UPDATE&lt;/span> user_id &lt;span style="color:#f92672">=&lt;/span> user_id;
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>Operações de &lt;strong>UPSERT&lt;/strong> são inerentemente idempotentes quando combinadas com uma &lt;em>unique constraint&lt;/em>, pois garantem que execuções repetidas levem ao mesmo estado final. Esse padrão é frequentemente a forma preferida de lidar com cenários de &lt;em>&amp;lsquo;check-then-act&amp;rsquo;&lt;/em> para inserções quando uma &lt;em>unique constraint&lt;/em> é definida.&lt;/p>
&lt;h3 id="74-pessimistic-locking">7.4 Pessimistic Locking
&lt;/h3>&lt;p>O &lt;strong>pessimistic locking&lt;/strong> assume que conflitos são prováveis e os evita adquirindo um bloqueio em um recurso antes de acessá-lo. Isso significa que outras operações que tentarem acessar o mesmo recurso serão bloqueadas até que o bloqueio seja liberado. Embora eficaz na prevenção de &lt;strong>race conditions&lt;/strong>, pode levar à redução da concorrência e a potenciais &lt;em>deadlocks&lt;/em> se não for gerenciado com cuidado.&lt;/p>
&lt;ul>
&lt;li>&lt;strong>Prós:&lt;/strong> Garante a consistência dos dados, relativamente simples de implementar para seções críticas.&lt;/li>
&lt;li>&lt;strong>Contras:&lt;/strong> Pode reduzir significativamente o &lt;em>throughput&lt;/em> do sistema, aumenta o risco de &lt;em>deadlocks&lt;/em> e pode ser complexo de gerenciar em sistemas distribuídos. Para mitigar &lt;em>deadlocks&lt;/em>, sempre adquira bloqueios em uma ordem consistente entre as transações e utilize &lt;em>timeouts&lt;/em> de bloqueio para evitar o bloqueio indefinido.&lt;/li>
&lt;/ul>
&lt;h3 id="75-optimistic-locking">7.5 Optimistic Locking
&lt;/h3>&lt;p>Em contraste com o &lt;em>pessimistic locking&lt;/em>, o &lt;strong>optimistic locking&lt;/strong> assume que os conflitos são raros. Em vez de bloquear recursos antecipadamente, ele permite que múltiplas operações prossigam concorrentemente. Quando uma operação tenta confirmar suas alterações, ela verifica se o recurso foi modificado por outra operação concorrente desde que foi lido inicialmente. Isso é normalmente alcançado usando um número de versão ou uma coluna de &lt;em>timestamp&lt;/em>. Crucialmente, essa verificação de versão deve, idealmente, ser imposta no nível do banco de dados para fornecer uma garantia real.&lt;/p>
&lt;p>Este padrão frequentemente envolve uma &lt;strong>atualização condicional&lt;/strong> no nível do banco de dados:&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;">&lt;code class="language-sql" data-lang="sql">&lt;span style="display:flex;">&lt;span>&lt;span style="color:#66d9ef">UPDATE&lt;/span> product
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#66d9ef">SET&lt;/span> quantity &lt;span style="color:#f92672">=&lt;/span> &lt;span style="color:#f92672">?&lt;/span>, &lt;span style="color:#66d9ef">version&lt;/span> &lt;span style="color:#f92672">=&lt;/span> &lt;span style="color:#66d9ef">version&lt;/span> &lt;span style="color:#f92672">+&lt;/span> &lt;span style="color:#ae81ff">1&lt;/span>
&lt;/span>&lt;/span>&lt;span style="display:flex;">&lt;span>&lt;span style="color:#66d9ef">WHERE&lt;/span> id &lt;span style="color:#f92672">=&lt;/span> &lt;span style="color:#f92672">?&lt;/span> &lt;span style="color:#66d9ef">AND&lt;/span> &lt;span style="color:#66d9ef">version&lt;/span> &lt;span style="color:#f92672">=&lt;/span> &lt;span style="color:#f92672">?&lt;/span>;
&lt;/span>&lt;/span>&lt;/code>&lt;/pre>&lt;/div>&lt;p>Após executar tal instrução &lt;code>UPDATE&lt;/code>, a aplicação verifica o número de linhas afetadas. Se exatamente uma linha foi afetada, a atualização foi bem-sucedida. Se zero linhas foram afetadas, isso indica que outra transação modificou o registro concorrentemente (ou seja, a &lt;code>version&lt;/code> na cláusula &lt;code>WHERE&lt;/code> não correspondia mais) e um conflito foi detectado. Nesse caso, a operação é normalmente revertida e o cliente é instruído a tentar novamente. Essa abordagem oferece maior concorrência, mas requer a implementação de lógica de tentativa (&lt;em>retry&lt;/em>) no lado do cliente.&lt;/p>
&lt;ul>
&lt;li>&lt;strong>Prós:&lt;/strong> Alta concorrência, evita &lt;em>deadlocks&lt;/em>.&lt;/li>
&lt;li>&lt;strong>Contras:&lt;/strong> Requer mecanismos de tentativa, conflitos podem levar a trabalho desperdiçado se as tentativas forem frequentes.&lt;/li>
&lt;/ul>
&lt;h3 id="76-idempotency-keys">7.6 Idempotency Keys
&lt;/h3>&lt;p>&lt;strong>Idempotency keys&lt;/strong> são um mecanismo poderoso, particularmente útil no design de APIs e sistemas distribuídos, principalmente para evitar o processamento duplicado de requisições (ex: devido a tentativas no lado do cliente ou problemas de rede). Uma operação idempotente é aquela que pode ser aplicada múltiplas vezes sem alterar o resultado além da aplicação inicial. Ao incluir uma &lt;em>idempotency key&lt;/em> única em cada requisição, o servidor pode detectar e ignorar requisições subsequentes com a mesma chave, garantindo que uma operação (ex: cobrar um cliente, criar um recurso) seja realizada apenas uma vez. As &lt;em>idempotency keys&lt;/em> são normalmente impostas usando uma &lt;em>unique constraint&lt;/em> no nível do banco de dados. É crucial entender que, embora as &lt;em>idempotency keys&lt;/em> evitem que requisições duplicadas causem efeitos duplicados, elas não substituem inerentemente os mecanismos adequados de controle de concorrência para gerenciar o estado compartilhado. Elas abordam o problema da entrega de mensagens duplicadas, não o acesso concorrente a um recurso compartilhado.&lt;/p>
&lt;h3 id="77-distributed-locks-avançado">7.7 Distributed Locks (Avançado)
&lt;/h3>&lt;p>Em sistemas distribuídos, onde múltiplos serviços ou instâncias podem estar tentando acessar o mesmo recurso compartilhado, os bloqueios tradicionais em processo são insuficientes. &lt;strong>Distributed locks&lt;/strong> são usados para coordenar o acesso entre diferentes processos ou máquinas. Ferramentas como Redis ou Apache ZooKeeper podem ser usadas para implementar mecanismos de bloqueio distribuído.&lt;/p>
&lt;p>Para instâncias simples de Redis de nó único, um bloqueio distribuído básico pode ser implementado usando o comando &lt;code>SET key value NX PX milliseconds&lt;/code>. Este comando define uma chave apenas se ela ainda não existir (&lt;code>NX&lt;/code>) e define um tempo de expiração (&lt;code>PX&lt;/code>), fornecendo um mecanismo básico de exclusão mútua. No entanto, essa abordagem é vulnerável se a instância do Redis falhar.&lt;/p>
&lt;p>Para implantações de Redis com múltiplos nós (ex: Redis Sentinel ou Cluster), algoritmos como &lt;strong>Redlock&lt;/strong> foram propostos para alcançar bloqueios distribuídos mais robustos. No entanto, é crucial notar que bloqueios distribuídos baseados em Redis, como o Redlock, são &lt;strong>controversos&lt;/strong> e devem ser usados com extrema cautela. Bloqueios distribuídos nunca devem ser a única linha de defesa para a consistência dos dados; a correção ainda deve ser garantida no nível do banco de dados. Eles introduzem uma complexidade significativa e suas garantias de correção sob vários cenários de falha são debatidas na comunidade de engenharia [1][2]. Implementar bloqueios distribuídos é significativamente mais complexo e introduz seu próprio conjunto de desafios, incluindo latência de rede, tolerância a falhas e garantia de consistência.&lt;/p>
&lt;ul>
&lt;li>&lt;strong>Aviso:&lt;/strong> Bloqueios distribuídos adicionam complexidade significativa e só devem ser considerados quando soluções mais simples não forem viáveis para ambientes distribuídos. Seja particularmente cauteloso com soluções como Redlock sem uma compreensão profunda de suas limitações e &lt;em>trade-offs&lt;/em>.&lt;/li>
&lt;/ul>
&lt;h2 id="8-escolhendo-a-abordagem-correta">8. Escolhendo a Abordagem Correta
&lt;/h2>&lt;p>Selecionar a estratégia apropriada para lidar com &lt;strong>race conditions&lt;/strong> envolve entender os &lt;em>trade-offs&lt;/em> entre consistência, desempenho e complexidade.&lt;/p>
&lt;p>Uma boa regra prática é: sempre que possível, empurre as garantias de correção para a camada do banco de dados em vez de depender da coordenação em nível de aplicação.&lt;/p>
&lt;ul>
&lt;li>&lt;strong>Race Conditions de Check-Then-Act (ex: Inserções Duplicadas, Venda Excessiva, Reservas Sobrepostas):&lt;/strong> Para esses cenários críticos, as &lt;strong>unique constraints&lt;/strong> do banco de dados são a solução primária e mais confiável para evitar inserções duplicadas. Ao lidar com mudanças de estado complexas ou alocação de recursos, mecanismos robustos como &lt;strong>transações com &lt;code>SELECT FOR UPDATE&lt;/code> (pessimistic locking)&lt;/strong> ou &lt;strong>distributed locks&lt;/strong> cuidadosamente implementados são frequentemente necessários para garantir a atomicidade e evitar &lt;strong>race conditions&lt;/strong>. A escolha aqui depende dos requisitos específicos de consistência, da natureza do recurso compartilhado e dos &lt;em>trade-offs&lt;/em> aceitáveis em desempenho e complexidade.&lt;/li>
&lt;li>&lt;strong>Race Conditions de Atualização (Lost Updates):&lt;/strong> Para evitar atualizações perdidas, o &lt;strong>optimistic locking&lt;/strong> é geralmente a abordagem preferida devido à sua maior concorrência, especialmente em aplicações web onde múltiplos usuários podem editar dados simultaneamente. Ele lida efetivamente com conflitos exigindo lógica de tentativa no lado do cliente.&lt;/li>
&lt;li>&lt;strong>Seções Críticas (exigindo exclusão mútua estrita):&lt;/strong> Quando a consistência absoluta dos dados é primordial para uma seção crítica curta e bem definida, o &lt;strong>pessimistic locking&lt;/strong> (ex: &lt;code>SELECT FOR UPDATE&lt;/code> em registros existentes) pode ser empregado. No entanto, deve-se estar agudamente ciente de seu impacto no &lt;em>throughput&lt;/em> do sistema e do risco aumentado de &lt;em>deadlocks&lt;/em>.&lt;/li>
&lt;/ul>
&lt;p>Sempre considere os requisitos específicos do seu sistema e o impacto potencial de cada solução no desempenho e na manutenibilidade.&lt;/p>
&lt;h2 id="9-lições-aprendidas-insight-pessoal">9. Lições Aprendidas (Insight Pessoal)
&lt;/h2>&lt;p>Meus encontros com &lt;strong>race conditions&lt;/strong> me ensinaram várias lições inestimáveis:&lt;/p>
&lt;ul>
&lt;li>&lt;strong>Race conditions são sutis e difíceis de reproduzir:&lt;/strong> Elas frequentemente se manifestam sob condições de carga específicas ou sequências de tempo raras, tornando-as notoriamente difíceis de depurar em ambientes de desenvolvimento.&lt;/li>
&lt;li>&lt;strong>Se a correção depende do tempo, seu sistema está contando com a sorte.&lt;/strong> Qualquer código que dependa da ordem precisa ou da velocidade de execução de operações concorrentes é um candidato principal para uma &lt;strong>race condition&lt;/strong>. Sempre questione as suposições sobre o tempo.&lt;/li>
&lt;li>&lt;strong>Prefira garantias em vez de suposições:&lt;/strong> Em vez de assumir que as operações são atômicas ou que fatores externos sempre se alinharão, construa sistemas que forneçam garantias explícitas de correção, seja por meio de &lt;em>constraints&lt;/em> de banco de dados, mecanismos de bloqueio ou operações idempotentes.&lt;/li>
&lt;li>&lt;strong>Evite depender de uma única camada para validação (quando possível):&lt;/strong> Quando o seu sistema utiliza garantias fortes em uma camada inferior (como database constraints), muitas vezes é vantajoso complementá-las com validação na aplicação (por exemplo, um check-before-insert). Essa verificação adicional não garante correção sob concorrência, mas pode reduzir operações desnecessárias no banco de dados e, mais importante, melhorar a experiência do usuário ao falhar mais rapidamente e fornecer feedback mais claro. Na prática, a camada inferior garante a correção, enquanto a camada de aplicação otimiza a usabilidade e a eficiência.&lt;/li>
&lt;/ul>
&lt;h2 id="10-conclusão">10. Conclusão
&lt;/h2>&lt;p>&lt;strong>Race conditions&lt;/strong> são um desafio inerente em sistemas concorrentes e distribuídos. Embora possam ser elusivas e frustrantes de depurar, entender suas causas e conhecer as várias estratégias para mitigá-las é crucial para construir software robusto e confiável. Ao impor a correção na camada correta — frequentemente o banco de dados — e escolher cuidadosamente os mecanismos de sincronização, os engenheiros de software podem lidar efetivamente com &lt;strong>race conditions&lt;/strong> e garantir a integridade de seus sistemas.&lt;/p>
&lt;h2 id="11-opcional-testando-race-conditions">11. Opcional: Testando Race Conditions
&lt;/h2>&lt;p>Testar para &lt;strong>race conditions&lt;/strong> é inerentemente difícil devido à sua natureza não determinística. No entanto, várias estratégias podem ajudar a descobri-las:&lt;/p>
&lt;ul>
&lt;li>&lt;strong>Requisições Paralelas:&lt;/strong> Simule múltiplas requisições concorrentes para o mesmo &lt;em>endpoint&lt;/em> ou recurso. Ferramentas como Apache JMeter ou &lt;em>scripts&lt;/em> personalizados podem ser usados para inundar o sistema com operações simultâneas.&lt;/li>
&lt;li>&lt;strong>Testes de Carga:&lt;/strong> Sujeitar o sistema a uma carga alta pode aumentar a probabilidade de &lt;strong>race conditions&lt;/strong> se manifestarem. Isso ajuda a identificar gargalos e áreas onde problemas de concorrência podem surgir.&lt;/li>
&lt;li>&lt;strong>Chaos Engineering:&lt;/strong> Introduzir intencionalmente latência ou falhas em um ambiente controlado pode, às vezes, expor &lt;em>bugs&lt;/em> dependentes do tempo que levam a &lt;strong>race conditions&lt;/strong>.&lt;/li>
&lt;/ul>
&lt;p>Embora desafiador, incorporar essas estratégias de teste em seu fluxo de trabalho de desenvolvimento pode melhorar significativamente a resiliência de suas aplicações contra &lt;strong>race conditions&lt;/strong>.&lt;/p>
&lt;h2 id="12-referências">12. Referências
&lt;/h2>&lt;p>[1] Kleppmann, M. (2016, 8 de fevereiro). &lt;em>How to do distributed locking&lt;/em>. &lt;a class="link" href="https://martin.kleppmann.com/2016/02/08/how-to-do-distributed-locking.html" target="_blank" rel="noopener"
>https://martin.kleppmann.com/2016/02/08/how-to-do-distributed-locking.html&lt;/a>&lt;/p>
&lt;p>[2] Antirez. (2016, 9 de fevereiro). &lt;em>Is Redlock safe?&lt;/em> &lt;a class="link" href="https://antirez.com/news/101" target="_blank" rel="noopener"
>https://antirez.com/news/101&lt;/a>&lt;/p></description></item></channel></rss>